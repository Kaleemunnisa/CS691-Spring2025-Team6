{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Frontend",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "export NVM_DIR=\"$HOME/.nvm\" && [ -s \"$NVM_DIR/nvm.sh\" ] && \\. \"$NVM_DIR/nvm.sh\" && nvm use 22 && npm start"
      ],
      "options": {
        "cwd": "${workspaceFolder}/frontend"
      },
      "problemMatcher": []
    },
    {
      "label": "Start Backend",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "cd ../venv && source bin/activate && cd ../app && python3 main.py"
      ],
      "options": {
        "cwd": "${workspaceFolder}/backend/app"
      },
      "problemMatcher": []
    }
  ]
}
